<li>
	{{place}}

	<button class="change-visit" data-id="{{id}}"data-newvisit="{{visit}}">
		{{#if visit}}Visit Again!{{else}}Been There!{{/if}}
	</button>

	<button id="trash" class="delete-museum" data-id="{{this.id}}"><i class="fas fa-dumpster"></i></button>
</li>
